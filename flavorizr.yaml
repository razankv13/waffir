app:
  android:
    flavorDimensions: "environment"

flavors:
  dev:
    app:
      name: "Waffir (Dev)"

    android:
      applicationId: "com.waffir.app.dev"

    ios:
      bundleId: "com.waffir.app.dev"

    macos:
      bundleId: "com.waffir.app.dev"
      buildSettings:
        LD_RUNPATH_SEARCH_PATHS:
          - "$(inherited)"
          - "@executable_path/../Frameworks"

  staging:
    app:
      name: "Waffir (Staging)"

    android:
      applicationId: "com.waffir.app.staging"

    ios:
      bundleId: "com.waffir.app.staging"

    macos:
      bundleId: "com.waffir.app.staging"
      buildSettings:
        LD_RUNPATH_SEARCH_PATHS:
          - "$(inherited)"
          - "@executable_path/../Frameworks"

  production:
    app:
      name: "Waffir"

    android:
      applicationId: "com.waffir.app"

    ios:
      bundleId: "com.waffir.app"

    macos:
      bundleId: "com.waffir.app"
      buildSettings:
        LD_RUNPATH_SEARCH_PATHS:
          - "$(inherited)"
          - "@executable_path/../Frameworks"
